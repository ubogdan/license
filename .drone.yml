kind: pipeline
name: test

platform:
  os: linux
  arch: amd64

#workspace:
#  base: /go
# path: src/code.gitea.io/gitea
  
steps:
  - name: build
    pull: always
    image: golang:1.13
    commands:
      - go test

kind: pipeline
name: build

platform:
  os: linux
  arch: amd64
